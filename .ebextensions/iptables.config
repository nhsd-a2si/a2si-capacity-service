container_commands:
  01_update_iptable:
    command: sudo iptables -t nat -A PREROUTING -i eth0 -p tcp  --dport 443 -j DNAT --to-destination $(sudo iptables -t nat -L | grep elasticbeanstalk | awk {'print substr($11,4)'})